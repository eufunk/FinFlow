import { ComingSoon } from "@/components/layout/ComingSoon";

export default function SettingsPage() {
  return (
    <ComingSoon
      title="Einstellungen"
      description="Hier wird künftig das Finanzprofil (Einkommen, Ausgaben, Notgroschen) bearbeitet - die API (GET/PUT /api/v1/profile) existiert bereits."
    />
  );
}
