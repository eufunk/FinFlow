import { ComingSoon } from "@/components/layout/ComingSoon";

export default function InsurancePage() {
  return (
    <ComingSoon
      title="Versicherungen"
      description="Im Backend gibt es bisher nur eine interne, lesende Anbindung für den Financial Health Score - ein eigenes Modul mit CRUD-Endpoints ist noch offen."
    />
  );
}
