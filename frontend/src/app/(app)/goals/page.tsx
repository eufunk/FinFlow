import { ComingSoon } from "@/components/layout/ComingSoon";

export default function GoalsPage() {
  return (
    <ComingSoon
      title="Sparziele"
      description="Eine Zusammenfassung ist bereits auf dem Dashboard sichtbar - eine eigene Verwaltungsseite (Ziel anlegen/bearbeiten) folgt als nächste Frontend-Slice."
    />
  );
}
