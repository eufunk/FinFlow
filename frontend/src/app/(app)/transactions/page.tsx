import { ComingSoon } from "@/components/layout/ComingSoon";

export default function TransactionsPage() {
  return (
    <ComingSoon
      title="Transaktionen"
      description="Die API (GET/POST /api/v1/transactions) ist bereits fertig - diese Ansicht folgt als nächste Frontend-Slice."
    />
  );
}
